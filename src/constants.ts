export const pagingSize = 5;
export const apiUrl = process.env.BACKEND_URI;
export const stompUrl = process.env.STOMP_URL;

export const SocailLoginConfig = {
  providers: [
    'amazon',
    'facebook',
    'github',
    'google',
    'instagram',
    'linkedin',
  ],
};
