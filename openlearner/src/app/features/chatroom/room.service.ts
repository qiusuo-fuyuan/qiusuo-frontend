import { OnInit } from "@angular/core";

class RoomService implements OnInit{
    
    /*onInit will need to initialize the room.Make sure that
    all the data are prepared before the room to be shown.Data that needs to be
    prepared
    Channels:
    Online Users:
    */
    ngOnInit() {

    }
}